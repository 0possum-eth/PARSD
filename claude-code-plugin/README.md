# Claude Code Plugin Variant: PARSD

This folder is the Claude Code plugin form of the PARSD package.

Includes:
- `.claude-plugin/plugin.json` metadata
- `skills/` full skills set
- `commands/` Arbiter command markdown
- `hooks/` session-start context hook
- `resonant-features/` full M1-M5 runtime source bundle

See `install-plugin.md` for install steps.
